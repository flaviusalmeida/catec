package br.com.catec.domain.usuario;

/** Valores alinhados a {@code usuario_perfil.perfil} (CHECK no Flyway V1). */
public enum PerfilMacro {
    COLABORADOR,
    ADMINISTRATIVO,
    SOCIO,
    SALA_TECNICA,
    CAMPO,
    FINANCEIRO
}
