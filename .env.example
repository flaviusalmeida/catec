# PostgreSQL (Docker Compose na raiz do repositório)
POSTGRES_DB=catec
POSTGRES_USER=catec
POSTGRES_PASSWORD=catec
POSTGRES_PORT=5433

# Mailpit (opcional — portas no host)
MAILPIT_SMTP_PORT=1025
MAILPIT_UI_PORT=8025
